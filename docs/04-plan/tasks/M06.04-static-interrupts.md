# Task M06.04: `interrupt_before:` / `interrupt_after:` in YAML
**ID:** M06.04  (matches ROADMAP # column and filename `M06.04-static-interrupts.md`)
**Milestone:** M06
**Status:** todo
**Priority:** medium
**Assigned:** unassigned
**Blocked by:** M06.01
**Effort:** S
**Created:** 2026-08-12
**Completed:** —

## Description

`interrupt_before` is the static alternative to calling dynamic `interrupt()` inside
a node function, and it is trivially expressible in YAML — which arguably makes it
the more natural fit for a declarative library. It lets a user add an approval pause
with no Python change at all.

```yaml
interrupt_before: [approval]
interrupt_after: []
```

## Acceptance Criteria

- [ ] `GraphConfig` gains `interrupt_before: list[str] | None` and `interrupt_after: list[str] | None`
- [ ] Both forwarded to the top-level `graph.compile()`
- [ ] Names cross-validated against declared nodes; unknown name raises `ConfigValidationError` with a closest-match suggestion
- [ ] Documented as requiring a checkpointer — a static interrupt without one cannot resume; surface a clear error or an explicit doc warning rather than a silent no-op
- [ ] Omitting both fields leaves the `compile()` call unchanged
- [ ] `schema/workflow.schema.json` regenerated alongside `tests/fixtures/`
- [ ] Test covers: pause fires at the declared node, and validation rejects an unknown node name

## Files

- `src/langgraph_declarative/schema.py` — `GraphConfig`, `cross_validate`
- `src/langgraph_declarative/builder.py` — `_build()`
- `schema/workflow.schema.json` — regenerate
- `tests/test_schema.py`, `tests/test_hitl.py` — extend

## Notes

Verified present on LangGraph 1.2.2 `compile()`.

Decide and document the interaction with subgraphs: these are top-level compile
arguments, so a node name inside a subgraph file is not addressable here. Keep
validation scoped to nodes declared in the current file.
